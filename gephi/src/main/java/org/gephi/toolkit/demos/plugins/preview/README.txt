This folder contains the structure of a basic preview plugin that does some simple rendering and also reacts to mouse events.
Just having the @ServiceProvider Annotations makes the plugin available and auto-included in any Preview window.

Run PreviewJFrame main to try this plugin.